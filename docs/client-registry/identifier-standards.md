# Identifier Standards

The Client Registry supports multiple identifier types to ensure accurate client matching across systems.

Common identifier types include:

- MRN (Medical Record Number)
- NIDA
- HCRCODE
- Insurance identifiers

Each client must have at least one preferred identifier.
